#include <iostream>
// #include "Zombie.hpp"
#include "ZombieEvent.hpp"

int		main(void) {
	// ZombieEvent		*event = new ZombieEvent;
	// Zombie			*zombie;

	std::cout << "Hello!" << '\n';
	// Define zombie with event generator
	std::cout << "setZombieType" << '\n';
	// event->setZombieType("brain lover");
	// Define zombie through ZombieEvent class
	std::cout << "newZombie" << '\n';
	// zombie = event->newZombie("Michel");
	// Make it announces it
	// zombie->announce();
	// Delete every zombies
	// delete event;
	// delete zombie;
}
